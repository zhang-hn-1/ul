# yolo12_yange4 版本分析

本文档专门分析 `runs/train/yolo12_yange4` 这一版模型，相比原始 YOLO12n 架构到底改了什么，以及这些改动带来了哪些收益和代价。

## 1. yange4 是什么版本？

从训练参数可以确认，`yolo12_yange4` 使用的是自定义模型文件 `ultralytics/cfg/models/12/yolo12_yange.yaml`，并在 EVD4UAV 单类车辆检测数据集上继续训练得到。

对应训练运行目录：

- `runs/train/yolo12_yange4/`

对应模型配置：

- `ultralytics/cfg/models/12/yolo12_yange.yaml`

对应原始基线结构：

- `ultralytics/cfg/models/12/yolo12.yaml`

## 2. 相比原始 YOLO12，yange4 改了什么？

结论先说：`yange4` 不是大改版，而是一个 **单点轻量化改造版**。

它和原始 YOLO12n 的绝大部分结构保持一致，包括：

- 输入下采样方式没有变
- P3、P4 检测路径没有变
- neck 中大部分 `A2C2f` 结构没有变
- 检测头仍然是 `P3/P4/P5` 三尺度输出，没有增加 P2 分支
- 特征融合方式仍然是标准 `Concat`

真正的核心改动只有一刀：

### 2.1 把 backbone 最深层的 `A2C2f` 替换成 `C3Ghost`

原始 YOLO12n 中，backbone 最后一层是：

```yaml
- [-1, 4, A2C2f, [1024, True, 1]] # 8
```

而 `yolo12_yange.yaml` 中改成了：

```yaml
- [-1, 4, C3Ghost, [1024, True]] # 8
```

也就是说：

- 改动位置只发生在最深层 P5 特征提取模块
- 你没有破坏浅层和中层的小目标主干表示
- 你保留了大部分原始 YOLO12 的表达能力
- 你用更轻的 `C3Ghost` 去替换原本更重的 `A2C2f`

### 2.2 这个改动的设计思路是什么？

这个思路其实非常明确：

- 浅层特征对小目标最敏感，所以尽量不动
- 深层特征语义更强，但计算量也更大
- 因此只在最深层做轻量化替换，尽量减少对小目标检测能力的破坏

换句话说，`yange4` 的目标不是“极限提精度”，而是：

> 用尽可能小的结构改动，换取更轻的模型和更快的推理速度，同时尽量保住检测性能。

## 3. 对应带来了什么变化？

结合 `uav_models_1.csv`，`yange4` 与基线 `yolo12_n2` 的对比如下：

| 指标          | yolo12_n2 | yolo12_yange4 |    变化 |
| ------------- | --------: | ------------: | ------: |
| AP_S          |    0.1766 |        0.1735 | -0.0031 |
| AR_S          |    0.2640 |        0.2889 | +0.0249 |
| mAP(0.5:0.95) |    0.7628 |        0.7520 | -0.0108 |
| AP_50         |    0.9507 |        0.9515 | +0.0008 |
| AP_75         |    0.8533 |        0.8501 | -0.0032 |
| Params (M)    |      2.57 |          2.03 |   -0.54 |
| GFLOPs        |      6.48 |          6.05 |   -0.43 |
| Latency (ms)  |      9.29 |          7.89 |   -1.40 |
| FPS           |    107.63 |        126.74 |  +19.11 |

## 4. yange4 的提升主要体现在哪里？

### 4.1 模型更轻了

这是 `yange4` 最明确的提升。

- 参数量从 `2.57M` 降到了 `2.03M`
- 绝对减少 `0.54M`
- 相对降幅约 `21%`

这说明你那一刀替换并不是“装饰性修改”，而是确实把模型做轻了。

### 4.2 计算量更低了

- GFLOPs 从 `6.48` 降到 `6.05`
- 绝对减少 `0.43`
- 相对降幅约 `6.6%`

虽然降幅没有参数量那么夸张，但对部署来说这是实打实的计算收益。

### 4.3 推理速度更快了

这是 `yange4` 最亮眼的工程收益。

- 延迟从 `9.29 ms` 降到 `7.89 ms`
- 降低了 `1.40 ms`
- 相对下降约 `15.1%`

同时：

- FPS 从 `107.63` 提升到 `126.74`
- 增加了 `19.11 FPS`
- 相对提升约 `17.8%`

这意味着 `yange4` 更适合实时性要求更高的部署场景。

### 4.4 小目标召回更好了

- `AR_S` 从 `0.2640` 提升到 `0.2889`
- 提升了 `0.0249`
- 相对提升约 `9.4%`

这说明 `yange4` 在小目标“找得到多少”这件事上反而更积极。

可以理解为：

- 虽然你只改了最深层结构
- 但轻量化后，模型在推理效率和部分召回能力上有收益

## 5. 哪些地方没有提升，甚至退化了？

这部分也需要实话实说，因为这恰恰能说明 `yange4` 的真实定位。

### 5.1 总体 mAP 下降了

- `mAP(0.5:0.95)` 从 `0.7628` 降到 `0.7520`
- 下降了 `0.0108`

这说明 `yange4` 并不是当前最优精度版本。

### 5.2 小目标 AP 没有提升

- `AP_S` 从 `0.1766` 降到 `0.1735`
- 略微下降 `0.0031`

这说明它虽然更容易“召回”小目标，但在小目标定位与精确匹配上没有超过原始模型。

### 5.3 高阈值定位精度略有下降

- `AP_75` 从 `0.8533` 降到 `0.8501`

这通常意味着：

- 预测框更容易覆盖到目标
- 但框的位置精细度没有原始模型稳定

## 6. 从 runs 里的训练过程看，yange4 表现如何？

从 `runs/train/yolo12_yange4/results.csv` 来看：

- 最终 `metrics/mAP50-95(B)` 收敛到 `0.84604`
- 最终 `metrics/mAP50(B)` 收敛到 `0.98554`
- 最终 `metrics/precision(B)` 为 `0.96759`
- 最终 `metrics/recall(B)` 为 `0.96573`

而基线 `runs/train/yolo12_n2/results.csv` 最终为：

- `metrics/mAP50-95(B) = 0.86061`
- `metrics/mAP50(B) = 0.98590`
- `metrics/precision(B) = 0.97283`
- `metrics/recall(B) = 0.96599`

这说明从训练日志本身看，`yange4` 的最终验证精度也低于基线，尤其是：

- `mAP50-95` 更低
- `precision` 更低
- `recall` 基本接近

所以训练过程和最终评测表给出的结论是一致的：

> `yange4` 的主要收益不是精度提升，而是轻量化和加速，同时尽量把精度损失控制在较小范围内。

## 7. 应该如何概括 yange4 的贡献？

如果你要写论文、汇报或者答辩，我建议你把 `yange4` 这一版这样定义：

> `yange4` 是一种针对 YOLO12 的单点轻量化改造版本。该方法仅在 backbone 最深层将原始 `A2C2f` 模块替换为更轻量的 `C3Ghost`，在尽量保持原始多尺度检测框架不变的前提下，降低模型参数量和计算量，加快推理速度，并在小目标召回上获得提升。

## 8. 最适合怎么评价 yange4？

最客观的评价不是“精度更高”，而是下面这句话：

> 相比原始 YOLO12n，`yange4` 用一次非常克制的结构替换，实现了更轻的模型、更低的计算量和更快的推理速度，同时显著提升了小目标召回能力；代价是总体 mAP 和小目标 AP 略有下降。因此，`yange4` 更适合作为 **轻量化部署优化版本**，而不是当前的 **最优精度版本**。

## 9. 一句话总结

`yange4` 的本质不是“提精度版 YOLO12”，而是：

> **通过只替换最深层重模块为 `C3Ghost`，换来更快速度、更少参数和更高小目标召回的轻量化版本。**
